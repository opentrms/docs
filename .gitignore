# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Walkthrough videos copied in for local preview by `npm run sync-videos`.
# Set LEARN_VIDEO_BASE_URL to publish them from a real host instead; committing
# them would add a fresh ~5 MB blob to history on every regeneration.
/static/learn/assets/**/*.mp4

# Playwright MCP scratch output (page snapshots, console logs) from driving
# the site in a browser locally.
/.playwright-mcp/

# Misc
.DS_Store
.claude/
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
